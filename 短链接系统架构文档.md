# 短链接系统架构文档

## 1. 项目概述

本项目是一个基于 Go-Zero 微服务框架的高性能短链接生成系统，支持分布式部署、高并发访问和智能缓存。系统采用前后端分离架构，提供完整的短链接生成、跳转、管理功能。

## 2. 整体架构设计

### 2.1 系统架构图

```mermaid
graph TB
    %% 用户层
    User[用户浏览器]
    
    %% 前端层
    Frontend[Vue3 前端应用<br/>Port: 5173]
    
    %% 负载均衡层
    LB[负载均衡器]
    
    %% API网关层
    Gateway[API Gateway<br/>shorturl-api<br/>Port: 8080]
    
    %% 微服务层
    RPC[RPC服务<br/>shorturl-rpc<br/>Port: 8888]
    
    %% 服务发现
    Etcd[Etcd集群<br/>Port: 2379]
    
    %% 缓存层
    Redis[Redis缓存<br/>Port: 6379]
    BloomFilter[Redis布隆过滤器]
    
    %% 数据库层
    MySQL[MySQL数据库<br/>Port: 3306]
    
    %% 监控日志
    Logs[日志系统]
    
    %% 连接关系
    User --> Frontend
    Frontend --> LB
    LB --> Gateway
    Gateway --> RPC
    RPC --> Etcd
    Gateway --> Etcd
    RPC --> Redis
    RPC --> BloomFilter
    RPC --> MySQL
    RPC --> Logs
    Gateway --> Logs
    
    %% 样式定义
    classDef frontend fill:#e1f5fe
    classDef api fill:#f3e5f5
    classDef rpc fill:#e8f5e8
    classDef storage fill:#fff3e0
    classDef cache fill:#fce4ec
    
    class Frontend frontend
    class Gateway api
    class RPC rpc
    class MySQL,Etcd storage
    class Redis,BloomFilter cache
```

### 2.2 技术栈

#### 前端技术栈
- **Vue 3**: 渐进式JavaScript框架
- **Vite**: 现代化构建工具
- **Axios**: HTTP客户端库
- **CSS3**: 现代化样式设计

#### 后端技术栈
- **Go-Zero**: 微服务框架
- **gRPC**: 高性能RPC通信
- **Gin**: HTTP Web框架（单体版本）
- **GORM**: ORM数据库操作
- **Protobuf**: 数据序列化

#### 基础设施
- **MySQL**: 关系型数据库
- **Redis**: 内存缓存数据库
- **Etcd**: 服务发现与配置中心
- **Docker**: 容器化部署

## 3. 微服务架构详解

### 3.1 服务拆分

```mermaid
graph LR
    subgraph "API Gateway Layer"
        API[shorturl-api<br/>HTTP接口服务]
    end
    
    subgraph "Business Logic Layer"
        RPC[shorturl-rpc<br/>业务逻辑服务]
    end
    
    subgraph "Data Access Layer"
        MySQL[(MySQL<br/>持久化存储)]
        Redis[(Redis<br/>缓存存储)]
        Bloom[布隆过滤器<br/>去重检测]
    end
    
    subgraph "Infrastructure Layer"
        Etcd[(Etcd<br/>服务发现)]
        Log[日志系统]
        Monitor[监控系统]
    end
    
    API --> RPC
    RPC --> MySQL
    RPC --> Redis
    RPC --> Bloom
    API --> Etcd
    RPC --> Etcd
    RPC --> Log
    API --> Log
```

### 3.2 服务通信

#### gRPC 接口定义

```protobuf
service ShortUrl {
    // 生成短链接
    rpc GenerateShortUrl(GenerateShortUrlRequest) returns (GenerateShortUrlResponse);
    // 处理短链接跳转
    rpc HandleShort(HandleShortRequest) returns (HandleShortResponse);
}
```

## 4. 核心业务流程

### 4.1 短链接生成流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant A as API Gateway
    participant R as RPC服务
    participant B as 布隆过滤器
    participant M as MySQL
    participant C as Redis
    participant S as 雪花算法
    
    U->>F: 输入原始URL
    F->>A: POST /generate
    A->>R: GenerateShortUrl RPC
    
    R->>R: IP限流检查
    alt 限流触发
        R-->>A: 返回限流错误
        A-->>F: HTTP 500 (code: 5001)
        F-->>U: 提示等待30分钟
    else 通过限流
        R->>B: 检查URL是否存在
        alt URL已存在
            R->>M: 查询已有短链
            M-->>R: 返回短链
        else URL不存在
            R->>S: 生成唯一ID
            S-->>R: 返回雪花ID
            R->>R: Base62编码
            R->>M: 保存到数据库
            R->>C: 保存到缓存
            R->>B: 添加到布隆过滤器
        end
        R-->>A: 返回短链
        A-->>F: 返回结果
        F-->>U: 显示短链
    end
```

### 4.2 短链接跳转流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant A as API Gateway
    participant R as RPC服务
    participant C as Redis
    participant M as MySQL
    
    U->>A: GET /{shortCode}
    A->>R: HandleShort RPC
    
    R->>R: 清理过期数据
    R->>C: 查询缓存
    alt 缓存命中
        C-->>R: 返回原始URL
    else 缓存未命中
        R->>M: 查询数据库
        alt 数据库有记录
            M-->>R: 返回原始URL
            R->>C: 更新缓存
        else 数据库无记录
            R-->>A: 返回404错误
        end
    end
    
    R-->>A: 返回原始URL
    A->>U: HTTP 302重定向
```

## 5. 核心组件设计

### 5.1 雪花算法ID生成器

```mermaid
graph LR
    subgraph "雪花ID结构 (64位)"
        T[41位时间戳] --> L[4位逻辑时钟]
        L --> M[10位机器ID]
        M --> S[8位序列号]
    end
    
    subgraph "特性"
        F1[时钟回拨容错]
        F2[分布式唯一性]
        F3[高性能生成]
        F4[趋势递增]
    end
```

**设计特点：**
- 支持时钟回拨容错机制
- 机器ID范围：0-1023
- 每毫秒可生成256个ID
- 理论上可使用69年

### 5.2 布隆过滤器

```mermaid
graph TB
    subgraph "布隆过滤器工作原理"
        URL[输入URL] --> H1[Hash1]
        URL --> H2[Hash2]
        URL --> H3[Hash3]
        URL --> H4[Hash4]
        URL --> H5[Hash5]
        URL --> H6[Hash6]
        URL --> H7[Hash7]
        
        H1 --> B[位数组]
        H2 --> B
        H3 --> B
        H4 --> B
        H5 --> B
        H6 --> B
        H7 --> B
        
        B --> R{判断结果}
        R -->|所有位都为1| E[可能存在]
        R -->|任一位为0| N[一定不存在]
    end
```

**配置参数：**
- 预期元素数量：1,000,000
- 误判率：1%
- 哈希函数：7个（MD5 + SHA1组合）
- 存储方式：Redis BF模块

### 5.3 缓存策略

```mermaid
graph TD
    subgraph "多级缓存架构"
        L1[Redis缓存<br/>热点数据]
        L2[MySQL数据库<br/>持久化存储]
        
        subgraph "缓存策略"
            S1[Cache-Aside模式]
            S2[TTL过期策略]
            S3[LRU淘汰策略]
        end
        
        L1 --> L2
    end
```

## 6. 数据模型设计

### 6.1 数据库表结构

```sql
CREATE TABLE `shorturl` (
  `id` bigint(20) unsigned NOT NULL COMMENT '雪花算法生成的唯一ID',
  `created_at` datetime(3) DEFAULT NULL COMMENT '创建时间',
  `updated_at` datetime(3) DEFAULT NULL COMMENT '更新时间',
  `deleted_at` datetime(3) DEFAULT NULL COMMENT '删除时间',
  `shorturl` varchar(20) NOT NULL COMMENT '短链接代码',
  `url` varchar(200) NOT NULL COMMENT '原始URL',
  PRIMARY KEY (`id`),
  KEY `idx_shorturl_deleted_at` (`deleted_at`),
  KEY `idx_shorturl_shorturl` (`shorturl`),
  KEY `idx_shorturl_url` (`url`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='短链接映射表';
```

### 6.2 Redis数据结构

```
# 短链接缓存
KEY: {shortCode}
VALUE: {originalURL}
TTL: 24小时

# 布隆过滤器
KEY: shorturl:Bloom
TYPE: BF (Redis Bloom Filter)
```

## 7. 性能优化设计

### 7.1 性能优化策略

```mermaid
graph TB
    subgraph "性能优化层次"
        A[应用层优化]
        C[缓存层优化]
        D[数据库优化]
        N[网络优化]
    end
    
    subgraph "具体措施"
        A --> A1[连接池复用]
        A --> A2[异步处理]
        A --> A3[批量操作]
        
        C --> C1[多级缓存]
        C --> C2[缓存预热]
        C --> C3[布隆过滤器]
        
        D --> D1[索引优化]
        D --> D2[分库分表]
        D --> D3[读写分离]
        
        N --> N1[gRPC通信]
        N --> N2[连接复用]
        N --> N3[数据压缩]
    end
```

### 7.2 限流保护

```mermaid
graph LR
    subgraph "限流策略"
        IP[IP级别限流]
        Rate[令牌桶算法]
        Window[滑动窗口]
    end
    
    subgraph "保护措施"
        DB[数据库保护]
        Cache[缓存保护]
        Service[服务保护]
    end
    
    IP --> DB
    Rate --> Cache
    Window --> Service
```

## 8. 监控与运维

### 8.1 监控体系

```mermaid
graph TB
    subgraph "监控层次"
        App[应用监控]
        Infra[基础设施监控]
        Business[业务监控]
    end
    
    subgraph "监控指标"
        App --> M1[响应时间]
        App --> M2[错误率]
        App --> M3[吞吐量]
        
        Infra --> M4[CPU使用率]
        Infra --> M5[内存使用率]
        Infra --> M6[磁盘IO]
        
        Business --> M7[短链生成量]
        Business --> M8[访问量]
        Business --> M9[缓存命中率]
    end
```

### 8.2 日志系统

```mermaid
graph LR
    subgraph "日志收集"
        API[API日志]
        RPC[RPC日志]
        DB[数据库日志]
        Cache[缓存日志]
    end
    
    subgraph "日志处理"
        Collect[日志收集]
        Parse[日志解析]
        Store[日志存储]
        Alert[告警通知]
    end
    
    API --> Collect
    RPC --> Collect
    DB --> Collect
    Cache --> Collect
    
    Collect --> Parse
    Parse --> Store
    Store --> Alert
```

## 9. 部署架构

### 9.1 容器化部署

```mermaid
graph TB
    subgraph "Docker容器"
        C1[shorturl-api容器]
        C2[shorturl-rpc容器]
        C3[MySQL容器]
        C4[Redis容器]
        C5[Etcd容器]
    end
    
    subgraph "编排工具"
        K8s[Kubernetes]
        Compose[Docker Compose]
    end
    
    subgraph "服务网格"
        Istio[Istio]
        Envoy[Envoy Proxy]
    end
```

### 9.2 高可用部署

```mermaid
graph TB
    subgraph "负载均衡层"
        LB1[负载均衡器1]
        LB2[负载均衡器2]
    end
    
    subgraph "应用层集群"
        API1[API服务1]
        API2[API服务2]
        RPC1[RPC服务1]
        RPC2[RPC服务2]
    end
    
    subgraph "数据层集群"
        M1[MySQL主库]
        M2[MySQL从库]
        R1[Redis主节点]
        R2[Redis从节点]
        E1[Etcd节点1]
        E2[Etcd节点2]
        E3[Etcd节点3]
    end
    
    LB1 --> API1
    LB1 --> API2
    LB2 --> API1
    LB2 --> API2
    
    API1 --> RPC1
    API1 --> RPC2
    API2 --> RPC1
    API2 --> RPC2
    
    RPC1 --> M1
    RPC2 --> M1
    M1 --> M2
    
    RPC1 --> R1
    RPC2 --> R1
    R1 --> R2
```

## 10. 安全设计

### 10.1 安全防护

```mermaid
graph TB
    subgraph "安全层次"
        Network[网络安全]
        App[应用安全]
        Data[数据安全]
    end
    
    subgraph "防护措施"
        Network --> N1[防火墙]
        Network --> N2[DDoS防护]
        Network --> N3[SSL/TLS]
        
        App --> A1[输入验证]
        App --> A2[SQL注入防护]
        App --> A3[XSS防护]
        
        Data --> D1[数据加密]
        Data --> D2[访问控制]
        Data --> D3[审计日志]
    end
```

## 11. 扩展性设计

### 11.1 水平扩展

```mermaid
graph LR
    subgraph "扩展维度"
        Compute[计算扩展]
        Storage[存储扩展]
        Cache[缓存扩展]
    end
    
    subgraph "扩展方案"
        Compute --> C1[服务实例扩展]
        Compute --> C2[负载均衡]
        
        Storage --> S1[分库分表]
        Storage --> S2[读写分离]
        
        Cache --> CA1[Redis集群]
        Cache --> CA2[一致性哈希]
    end
```

## 12. 配置管理

### 12.1 配置文件结构

```yaml
# API服务配置
Name: shorturl-api
Host: 0.0.0.0
Port: 8080
ShortUrlRpc:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: shorturl.rpc

# RPC服务配置
Name: shorturl.rpc
ListenOn: 0.0.0.0:8888
Etcd:
  Hosts:
    - 127.0.0.1:2379
  Key: shorturl.rpc
Mysql:
  DbUser: root
  DbPort: "3306"
  DbPass: root
  DbName: shorturl
  DbHost: localhost
BizRedis:
  RedisHost: 127.0.0.1
  RedisPort: "6379"
  RedisPass: ""
  RedisDB: 0
```

## 13. 总结

本短链接系统采用现代化的微服务架构设计，具备以下核心优势：

1. **高性能**: 通过多级缓存、布隆过滤器、雪花算法等技术实现高性能
2. **高可用**: 微服务架构支持服务独立部署和故障隔离
3. **可扩展**: 支持水平扩展，可根据业务需求灵活调整
4. **易维护**: 清晰的分层架构和标准化的开发规范
5. **安全可靠**: 完善的安全防护和监控体系

系统设计充分考虑了互联网应用的特点，能够支撑大规模用户访问和高并发场景，为业务发展提供稳定可靠的技术支撑。